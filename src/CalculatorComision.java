/**
 * Interfata pentru un calculator de comision
 * specific design pattern-ului Strategy
 */
public interface CalculatorComision {
    double calculate(Client client, double valTranzactionata);
}
